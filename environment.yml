name: uav_data_generation_test
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch=2.4
  - torchvision=0.19
  - pytorch-cuda=12.1
  - pip:
      - -e .[all]
