name: gen_eval
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch==2.0.0
  - torchvision=0.15.0
  - pytorch-cuda=11.8
  - pip:
      - -e git+https://github.com/layer6ai-labs/dgm-eval.git#egg=dgm_eval --src thirdparty
      - torch-fidelity
